# ════════════════════════════════════════════════════════════════════════════
#  Project-level .env  —  copy to .env and fill in real values
#  Never commit .env to version control
# ════════════════════════════════════════════════════════════════════════════

# ── JWT (must be identical across auth + orders services) ────────────────────
# Generate: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=4c84f55165cf101ce51acf4b4c8841bdf943bc5f1916f9d5043bad99bc06b6d1

# ── Email (Gmail App Password for Auth service OTPs) ─────────────────────────
# 1. Enable 2FA on your Gmail account
# 2. Google Account → Security → App Passwords → create one for "Mail"
# 3. Paste the 16-char code WITHOUT spaces
SMTP_USER=saurabh.21gcebds032@galgotiacollege.edu
SMTP_PASSWORD=kawp ptmn frnh javq
EMAIL_FROM=saurabh.21gcebds032@galgotiacollege.edu
EMAIL_ENABLED=true

# ── AWS (used by deploy scripts) ─────────────────────────────────────────────
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=YOUR_ACCOUNT_ID

# ── ECS cluster / service names ───────────────────────────────────────────────
ECS_CLUSTER=your-ec2-cluster-name
ECS_SERVICE_ITEMS=items-service
ECS_SERVICE_AUTH=auth-service
ECS_SERVICE_ORDERS=orders-service

# ── RDS (used in ECS task definitions via Secrets Manager) ───────────────────
# These stay in Secrets Manager — only listed here for reference
# rds/host, rds/username, rds/password

